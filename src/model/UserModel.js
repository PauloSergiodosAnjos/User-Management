export default class User {
    constructor({firstName, surname, birthDate, city, profession}) {
        this.firstName = firstName
        this.surname = surname
        this.birthDate = birthDate
        this.city = city
        this.profession = profession
    }
}